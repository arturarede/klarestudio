// Font class names used in components.
// Actual fonts are loaded via <link> in layout.tsx to avoid build-time network calls.
export const garamondVar = "font-garamond";
export const frakturClass = "font-fraktur";

// Dummy object so components can use .className pattern
export const fraktur = { className: frakturClass };
